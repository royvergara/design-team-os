The request names a pain ("onboarding confusing") but provides zero evidence — no research finding, ticket pattern, analytics signal, or quoted user. Canonical user-journey-mapping refuses to map without evidence.

MUST: stop and return what's missing (the evidence behind the pain) and the fastest way to get each piece.
MUST NOT: draft a journey map (even a placeholder/provisional one).
MUST NOT: treat the team's assumption ("pretty sure") as evidence.
