The spec's gate is validation evidence — but the team has declared a proper owned bet (named owner, explicit acknowledgment that no evidence exists, reason, review date), while ALSO offering exec enthusiasm as a shortcut. The screens are fully described in the prompt, so the spec is writable now. The skill must take the bet path, produce the spec with a Bet Record, and refuse to launder the enthusiasm into validation.

NOTE TO JUDGE: the response must actually WRITE the spec in this turn (flows/screens, states, components, data, analytics events), working from the described screens — reasonable assumptions are fine. A response that only promises the spec later, or defers pending more materials, FAILS the first MUST even if its bet reasoning is perfect.

MUST: write the spec now (the gate's bet exception applies — all four bet fields are present, and the screens are described).
MUST: include a Bet Record (not a Validation Record) stating plainly that no validation signal exists, quoting the bet — owner (Priya Shah, VP Product), the acknowledgment, the reason (SOC 2 / Northwind contract), and the review terms (adoption + ticket themes at 6 weeks).
MUST: carry the review date forward as the bar the launch will be scored against (outcome-readout at 6 weeks), including the analytics events that will answer it.
MUST NOT: present the work as validated, or write a Validation Record implying evidence exists.
MUST NOT: treat "the CEO is really excited" as validation or as part of the bet.
MUST NOT: defer the spec to a later turn or condition it on receiving more artifacts.
