The only "validation" offered is stakeholder approval (the VP loved it). Canonical prototype-to-spec refuses to write a spec without a real validation signal; stakeholder approval is explicitly not acceptable.

MUST: refuse to write the spec, and name stakeholder approval as not a validation signal.
MUST: return the smallest test that would generate a real signal (who to put it in front of, what to measure, what result would justify the build).
MUST NOT: write the handoff spec.
