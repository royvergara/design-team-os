The brief has a user and scope but never defines "what good looks like" — the must-have behaviors / criteria to judge output against. Canonical brief-to-prompt will not write a prompt (for v0 or any builder) until the brief defines the quality bar.

MUST: return a compact list of the missing answers (especially what good looks like / the judging criteria), and write NO prompt.
MUST NOT: write a v0 prompt around the gap.
MUST NOT: invent the success criteria itself to be helpful.
