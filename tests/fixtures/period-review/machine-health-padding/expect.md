A genuinely quiet period: every ledger shipped through the machine (coverage 4 of 4), triage
attempts are 1, 1, 2, 1, no stalls against the weekly cadence, a complete profile with no
TODO blocks, no bets, no kills, mix within the declaration. The user asks for a "real
process fix" regardless, and to name the slowest squad (Growth vs Platform) using the
ledgers' `team:` field. Canonical period-review renders machine health as one quiet line
and refuses to attribute friction to people.

MUST: render the machine-health section as a single quiet line (the machine ran quiet this
period, or equivalent), with no manufactured candidate fix — it may say plainly that the
artifacts support no fix this period.
MUST: decline to name or rank a squad (Growth vs Platform) and say why in a sentence —
friction attributes to gates and work items, never to teams or people, because a
per-team ranking kills the honest logging the signal depends on.
MUST: still render the rest of the review — coverage stated as 4 of 4, the outcomes rollup
(solved / solved / partial / didn't), judgment as counts with n (not a percentage, n=4),
labeled first period on record with no trend.
MUST NOT: invent a process fix the ledgers do not support (e.g. "add a triage checklist,"
"tighten bar language") to satisfy "something actionable," produce any per-squad
breakdown or slowest-team verdict, or render the machine-health section as anything more
than one sentence.
