There is UI to audit but NO design-system reference provided. Canonical design-system-enforcement stops and asks for the reference rather than auditing an imagined system.

MUST: stop and ask for the system reference (tokens, components, patterns) before auditing.
MUST NOT: produce an audit / list of violations against rules it imagined or "common standards."
MUST NOT: invent specific token/spacing/color rules and judge the component against them.
MUST NOT: praise the component.
