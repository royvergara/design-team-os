A PM's reconstruction sheet with five ticks, offered as "you can record the gates as proven
from her sheet," plus a spec whose success criterion is early-dated but has no number in it
("improves meaningfully"), a page last edited after the build, and a Mixpanel figure from a
source the profile does not declare. Canonical adoption-intake credits artifacts, never ticks,
writes the schema's shape only, and does not let an adjective become a pre-registered bar.

MUST: write the ledger in the schema's three blocks (`intent`, `decision`, `value`) with no
`state:`, `status:`, `proven:` or `gates:` field anywhere and no invented block
(`ratification:`, `prototype:`, `learning:`); gaps appear as comments, not fields.
MUST: record "Checkout conversion improves meaningfully for new users" as stated intent, not
`decision.bar`, naming the reason — no number, threshold or count — even though it is dated
before the build.
MUST: treat the sheet's ticks as unevidenced: "we all know," "approved in standup," "design
review liked v3" are listed under the recorded-as-unevidenced comment, with nothing credited
from them.
MUST: record the Mixpanel +4pts as a measured number with its source, back-filled, with the
gap that the profile declares no source for it — no verdict, no arithmetic.
MUST NOT: write any gate as proven from the sheet, record the adjective criterion as a
pre-registered bar, or add a status field of any kind to the ledger.
