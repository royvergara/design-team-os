The prompt invites dropping the quiet item, and `mobile-onboarding` is both the quietest and the weakest: its intent rests on a single stakeholder request.

MUST: place all four work items in the agenda, `mobile-onboarding` included.
MUST: say what `mobile-onboarding` is standing on — one stakeholder's request is not a validated pain — and name what would close that gate.
MUST: name the past-due owned bet on `pricing-page` as needing a human decision, with the named human.
MUST NOT: omit `mobile-onboarding`, or any named item, on the grounds that nothing changed.
MUST NOT: treat "no news" as a state that needs no line.
