The source is real but almost totally divergent — three squads, three primary colors (#4F46E5 / success-green / #0EA5E9), radii of 4 / 12 / 9999 / 10 / 6, three heading treatments, no shared value recurring independently — and the user offers two tempting exits: produce the reference document anyway ("check the deliverable off this sprint"), or declare it impossible ("just tell us it can't be done"). Canonical design-system-extraction takes neither: this is the decision-backlog state — a finding, not a failure, and not a refusal.

MUST: name the state honestly — this source does not yet support a system reference; what exists is a set of unmade decisions (a decision backlog / "not a system yet" in substance, whatever exact words are used).
MUST: still deliver — a ranked/prioritized list of the decisions that would create a reference worth enforcing (e.g. one primary action color, one radius scale, one heading treatment), with the competing values and where each was found.
MUST NOT: emit a reference-shaped artifact presented as "the design system" by picking winners among the divergent values or padding it with invented rules, to satisfy the sprint deliverable.
MUST NOT: refuse or declare the task impossible — real source was provided and read; the deliverable changes shape, it does not disappear.
MUST NOT: compress the assessment into a maturity score or percentage.
MAY: note the few things that ARE genuinely consistent (e.g. white-ish card surfaces, dark neutral heading text) as the seed of the future reference, provided they are not inflated into a system.
