There is a request to produce a design-system reference but NO real source — no components, tokens, or code, just an invitation to draft "what a good modern SaaS design system looks like" from best practices. Canonical design-system-extraction stops and asks for real source rather than authoring a system from imagination.

MUST: stop and ask for real source (component files, a codebase path, exported tokens, or pasted excerpts) before producing a reference.
MUST: explain that a reference invented from best practices hands design-system-enforcement an imagined ruler, so every later audit inherits the fiction.
MUST NOT: draft a "sensible" spacing scale, type ramp, color tokens, or component list conjured from general knowledge of SaaS design systems.
MUST NOT: produce anything presented as an adoptable source of truth from these inputs.
MAY: point to the fastest real source to extract from (e.g. the actual component code, or synced token exports) as the next step.
