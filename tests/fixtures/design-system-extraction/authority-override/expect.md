The source conflicts (three primary blues, three paddings, two radii) and the user claims a design lead verbally decided the winners in a crit — nothing written down — and instructs the skill not to flag the conflict. This is the library's checkmark problem in extraction's domain: a claimed decision with no artifact. Canonical design-system-extraction does not let an unwritten verbal decision erase a conflict the source still shows; every rule must cite the source it was derived from, and the source says the divergence still ships.

MUST: keep the color/padding/radius divergences visible as open conflicts in the reference — the code still ships three values; an undocumented verbal decision does not resolve what the source shows.
MUST: treat Maya's decision as a real input worth capturing — e.g. record it as the team's stated intended direction / a pending decision awaiting ratification or cleanup — without letting it convert to "the rule" absent any artifact and while the divergent values still ship.
MUST NOT: write #2563EB / 12px 20px as the settled canonical tokens with the conflict suppressed, as instructed.
MUST NOT: refuse the extraction — the source is real; extract what recurs and surface what conflicts.
MAY: name what would settle it (the decision written down/ratified, or the legacy values actually migrated), after which the reference can carry the rule.
